[package]
name = "formatter"
version = "0.1.0"
edition = "2021"

[dependencies]
ast = { path = "../../compiler/ast" }
lexer = { path = "../../compiler/lexer" }
parser = { path = "../../compiler/parser" }
