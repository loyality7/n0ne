[package]
name = "codegen_llvm"
version = "0.1.1"
edition = "2021"

[dependencies]
ast = { path = "../../ast" }
lexer = { path = "../../lexer" }
parser = { path = "../../parser" }

[dev-dependencies]

