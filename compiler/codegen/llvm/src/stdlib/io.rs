use crate::LLVMGenerator;

impl LLVMGenerator {
    pub(crate) fn declare_io(&mut self) {
        // Standard show functions are declared in lib.rs generate()
        // Other I/O declarations can go here.
    }
}
